# Copiar a .env en el VPS (nunca subir .env al repositorio)
# docker stack deploy lee estas variables al interpretar stack.yml

# --- Imagen ---
IMAGE_TAG=latest
REPLICAS=1

# --- Flask ---
SECRET_KEY=cambiar-por-clave-segura-larga

# --- PostgreSQL ---
POSTGRES_DB=sise_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Sise2026

# --- Correo (Gmail u otro SMTP) ---
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=hernymejia@gmail.com
MAIL_PASSWORD=wovytcycsqboesgl
MAIL_DEFAULT_SENDER_NAME=Aviso Conjunto Esperanza
MAIL_DEFAULT_SENDER_EMAIL=tu-correo@gmail.com

# --- Desarrollo local (opcional) ---
# DATABASE_URL=postgresql://postgres:password@localhost:5432/sise_db
